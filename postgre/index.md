sudo -u postgres -i --- залогиниться под пользователя postgres \
psql -d(db_name) postgres -U(user) postgres --- залогиниться в оболочку psql для бд postgres под пользоватлем postgres
\c - проверка пользователя